# Extra dependency for the DiffGRM (masked diffusion) baseline on top of
# requirements-sft.txt. Everything else it imports is already present.
tiktoken==0.12.0
# genrec's tokenizer imports sentence_transformers at module load, even with
# sid_quantizer=external where no sentence embeddings are computed.
sentence-transformers==3.4.1
